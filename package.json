{
  "name": "the-100-game",
  "version": "1.0.0",
  "description": "Monorepo root for The 100 Game",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm start --prefix client",
    "start": "npm run dev:server"
  }
}
