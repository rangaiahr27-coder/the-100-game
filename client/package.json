{
  "name": "the-100-game-client",
  "version": "1.0.0",
  "description": "The 100 Game - Baseball Trivia Frontend",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "start:prod": "serve -s build -l $PORT"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "serve": "^14.2.6",
    "socket.io-client": "^4.7.2"
  },
  "proxy": "http://localhost:3001",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
